# All units are in SI
# ===================
# Time:     second, hz
# Angle:    radian
# Distance: meter

basestation_gps_driver:
  ros__parameters:
    # TODO: update thus
    port: /dev/gps
    baud: 38400

rover_gps_driver:
  ros__parameters:
    # TODO: update thus
    port: /dev/gps
    baud: 38400

gps_linearization:
  ros__parameters:
    world_frame: "map"
    rover_frame: "base_link"

    # Exception, these are in degrees
    ref_lat: 38.4225202
    ref_lon: -110.7844653
    ref_alt: 0.0

pose_filter:
  ros__parameters:
    world_frame: "map"
    rover_frame: "base_link"

imu_driver:
  ros__parameters:
    frame_id: "base_link"
    update_rate: 30
