# dbc_runtime Library

The `dbc_runtime` library is a runtime library for encoding and decoding DBC messages.

See the `tests/` directory for usage examples and test cases. More documentation will be added soon.

